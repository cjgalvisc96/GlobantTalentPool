"""
Advantages over the LISTS:
    * The tuples are more faster and using less memory than the lists

Features:
    * Are immutables (cannot change once created)
"""

if __name__ == '__main__':
    _tuple_one = (1,)  # used "," to differentiate () operator
    _tuple_two = ("a", "b", "c", "d")
    dir(_tuple_one)
